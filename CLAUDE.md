# FluentFlow Local Claude Entry

Follow `AGENTS.md`. This file is the Claude-compatible pointer so repository
boundaries do not drift.

Before implementation work, read `docs/edition_boundaries.md`. This is the
independent Local repository, not an exported tree from FluentFlow Hosted.

Default language: Chinese. Code, commands, variables, and commit messages use
English. Never push, deploy, tag, release, or change a release version unless
explicitly asked.
